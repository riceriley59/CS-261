#include <stdio.h> //standard IO

int main(int argc, char** argv){ // parameters for command-line arguments
    printf("hello world.\n"); //output printing function from stdio library
    
    int x = 8;
    printf("This is the value of x: %d\n", x);

    //%d - int
    //%f - double or float (casted as double)
    //%c - char
    //%s - null-terminated string
    //%p - address value or pointer

    

    return 0;
}